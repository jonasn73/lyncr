import { TeamWorkspaceView } from "@/components/workspace-views/team-workspace-view"

/** Owner Team hub — phone contacts + app invites for who can answer calls. */
export default function TeamRoute() {
  return <TeamWorkspaceView />
}
