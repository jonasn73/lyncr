# Copy to .env.local and fill in values.
# Never commit .env.local (it's in .gitignore).

# Database - use Neon (free): https://neon.tech → New Project → Connection string → URI
# Paste the full URI here (it looks like this, with your real host/password):
# DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require
#
# If you see "password authentication failed for user postgres", you're still using
# the old local URL - replace DATABASE_URL in .env.local with your Neon URI above.
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/db_name

# Optional: in-app Help & Support / Security & Privacy (Settings links)
# When set, Security & Privacy and Help & Support open these URLs in a new tab.
# When not set, they open the in-app /privacy and /support pages.
# NEXT_PUBLIC_PRIVACY_POLICY_URL=https://your-domain.com/privacy
# NEXT_PUBLIC_SUPPORT_URL=https://your-domain.com/help
# NEXT_PUBLIC_SUPPORT_EMAIL=support@your-domain.com  (used on /support page when NEXT_PUBLIC_SUPPORT_URL is not set)
# Mobile: set EXPO_PUBLIC_PRIVACY_POLICY_URL and EXPO_PUBLIC_SUPPORT_URL in mobile/.env or EAS env

# Twilio (optional for call routing)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Session signing (use: openssl rand -base64 32)
# SESSION_SECRET=

# Telnyx (voice + numbers — platform carrier wallet; enable auto-recharge in Mission Control)
# TELNYX_API_KEY=
# TeXML account id (Mission Control → Account SID) — required for Decline → voicemail redirect.
# TELNYX_ACCOUNT_SID=
# Outbound SMS "from" DID (E.164). First non-empty wins; workspace purchased lines still preferred at send time.
# TELNYX_PHONE_NUMBER=+1XXXXXXXXXX
# TELNYX_MESSAGING_FROM_E164=+1XXXXXXXXXX
# TELNYX_OUTBOUND_NUMBER=+1XXXXXXXXXX
# Optional: TELNYX_MESSAGING_PROFILE_ID=  (auto-created by lib/telnyx-sms when unset)

# Stripe (Starter subscription + carrier credit packs + job PaymentIntents + Connect payouts)
# STRIPE_SECRET_KEY=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=   # pk_live_… / pk_test_… required for tech Manual Card Entry (Payment Element)
# STRIPE_TERMINAL_LOCATION_ID=         # optional tml_… for Tap to Pay (mobile); auto-created per user if unset
# STRIPE_WEBHOOK_SECRET=               # whsec_… — point Stripe at /api/webhooks/stripe (enable Connected account events)
# STRIPE_STARTER_PRICE_ID=           # Live Starter plan price_… ($49/mo)
# STRIPE_CORE_PRICE_ID=              # Legacy alias for STRIPE_STARTER_PRICE_ID
# TECH_JOB_COMMISSION_RATE=1         # 0–1 fraction of job charge credited to tech wallet (default 1 = 100%)
# LYNCR_PAYMENT_FEE_BPS=290          # Platform fee basis points on Collect Payment (290 = 2.9%)
# LYNCR_PAYMENT_FEE_FLAT_CENTS=30    # Platform flat fee in cents ($0.30)
# Enable Connect Express in Stripe Dashboard; complete Settings → Get paid in the app.
# TELNYX_AI_ASSISTANT_ID=
# Optional LLM id for new assistants — see GET https://api.telnyx.com/v2/ai/models (default: openai/gpt-4o; avoid openai/gpt-4o-mini — often blocked for Voice AI)
# TELNYX_AI_DEFAULT_MODEL=
# Optional TTS voice string, e.g. Telnyx.KokoroTTS.af_heart
# TELNYX_AI_VOICE=

# Realtime receptionist HUD (Pusher Channels — free tier is plenty)
# Create a free app at https://dashboard.pusher.com → Channels → Create app.
# Copy the four "App Keys" values. The two NEXT_PUBLIC_* vars must be set at BUILD time.
# When these are unset, the receptionist HUD silently falls back to its 15s polling refresh.
# PUSHER_APP_ID=
# PUSHER_KEY=
# PUSHER_SECRET=
# PUSHER_CLUSTER=us2
# NEXT_PUBLIC_PUSHER_KEY=            # same value as PUSHER_KEY
# NEXT_PUBLIC_PUSHER_CLUSTER=us2     # same value as PUSHER_CLUSTER

# Optional: dev-only login when database is not connected (development only)
# Use these credentials on the login form to get in without a DB.
# DEV_LOGIN_EMAIL=you@example.com
# DEV_LOGIN_PASSWORD=yourpassword

# Geocoding + driving directions (optional — OSRM fallback when unset)
# GOOGLE_GEOCODING_API_KEY=
# GOOGLE_MAPS_API_KEY=

# Map basemap (optional — Mapbox dark-v11; omit for free CARTO dark raster)
# NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
# NEXT_PUBLIC_MAP_TOKEN=

# Vehicle key reference (intake sheet FCC / key photos)
# KEY_REFERENCE_CACHE_ONLY=true   # Production: never call fccid.io at runtime (use bundled CSV + JSON + public/key-images/)
# Refresh data locally: npm run build:fcc-cache  (re-scrape + mirror new images)
# Mirror images only:  npm run mirror:fcc-images (download photos for existing cache JSON)
